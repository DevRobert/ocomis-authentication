function login(request, reply) {
    reply("Login: Not implemented.")
}

function logout(request, reply) {
    reply("Logout: Not implemented.")
}

function refresh(request, reply) {
    reply("Refresh: Not implemented.")
}

module.exports = {
    login,
    logout,
    refresh
}